export default [<% manufacturers.forEach(function(manufacturer){ %>
  { name: `<%- manufacturer.name %>`, country: '<%- manufacturer.country %>', activities: [<% manufacturer.activities.forEach(function (activity, i, activities) { %>'<%- activity %>'<%= i == activities.length-1 ? '' : ', ' %><% }); %>], <%if (manufacturer.background) { %>background: `<%- manufacturer.background %>`,<% } %>},<% }); %>
]
