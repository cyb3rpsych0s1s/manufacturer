import { <%- name %>, from, to } from './<%- filename %>'
describe('<%- filename %>', () => {
  it('from', () => {
    <% entries.forEach(function(entry){ -%>
    expect(from(<%- name %>.<%- entry.key %>)).toBe('<%- entry.value %>')
    <% }); -%>
  })
  it('to', () => {
    <% entries.forEach(function(entry){ -%>
    expect(to('<%- entry.value %>')).toBe(<%- name %>.<%- entry.key %>)
    <% }); -%>
    expect(to('unknown')).toBe(undefined)
  })
})
