import { <%- name %>, from, to, validator } from './<%- filename %>'
describe('<%- filename %>', () => {
  it('from', () => {
    <% entries.forEach(function(entry){ -%>
    expect(from(<%- name %>.<%- entry.key %>)).toBe('<%- entry.value %>')
    <% }); -%>
  })
  it('to', () => {
    <% entries.forEach(function(entry){ -%>
    expect(to('<%- entry.value %>')).toBe(<%- name %>.<%- entry.key %>)
    <% }); -%>
    expect(to('unknown')).toBe(undefined)
  })
  it('validator', () => {
    <% entries.forEach(function(entry){ -%>
      expect(validator.validate('<%- entry.value %>')).not.toHaveProperty('error')
    <% }); -%>
    expect(validator.validate('unknown')).toHaveProperty('error')
    expect(validator.validate(undefined)).toHaveProperty('error')
  })
})
